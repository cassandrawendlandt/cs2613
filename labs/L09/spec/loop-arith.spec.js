var arith=require ("../loop-arith.js");

describe("add",
    function() {
        it("1 + 1 = 2",
            function() {
                expect(                ).toBe(2);
            });

        it("0 + x = x",
            function() {

            });
    });

describe("mult",
    function() {
        it("0 * 2 = 0",   );

        it("1 * 2 = 2",
            function() {
                expect(arith.mult(1,2)).toBe(   );
            });

        it("2 * 2 = 4", );
    });
